/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['www.bing.com', 'img-s-msn-com.akamaized.net', 'th.bing.com', 'img1.hscicdn.com'],
    },
}

module.exports = nextConfig
